package com.example.textures;

import java.io.FileInputStream;

public class TextureLoader {
    //public static Texture getTexture("PNG", new FileInputStream(fileName));
    public static Texture getTexture(String extension, FileInputStream fs)  {
        return new Texture();
    }
}
